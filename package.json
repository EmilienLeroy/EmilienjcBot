{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --parallel",
    "dev:bot": "npm run dev -- --scope bot",
    "dev:robot": "npm run dev -- --scope robot"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
